{
  "name": "word-worm-game",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/style.css --minify && node bump-version.js",
    "sync:ios": "tailwindcss -i ./src/input.css -o ./dist/style.css --minify && rsync -a --delete --delete-excluded --exclude-from=capacitor-www-exclude.txt ./ www/ && npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "playwright": "^1.61.0",
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
